(const dim      [window.innerWidth, window.innerHeight];
)
(const size 5)
(set module.exports
     'height 100
     'width  100
     'size size
     'angle-zoom 100
     'noise-z 100
     'field-force 100
     'dimensions dim
     'columns (Math.round (/ (first dim) size))
     'rows (Math.round (/ (second dim) size))
     )
