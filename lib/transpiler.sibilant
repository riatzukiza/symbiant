(def transpile (path )

  (print path "changed")

  (let target (pipe path
                    (.replace  "/sibilant/" "/")
                    (.replace ".sibilant" ".js")))

  (pipe Sibilant
        (.from-file path)
        (.transpile)
        (.save-file target)))
