(include "./shell")

(.then (shell
        (git add ".")
        (git commit "-m" "\"preping for heroku\"")
        (git push origin master)
        (git push heroku master))
       (aprint "success"))
